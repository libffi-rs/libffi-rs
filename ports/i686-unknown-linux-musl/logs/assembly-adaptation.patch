--- /home/ubuntu/libffi-rs/libffi-sys-rs/libffi/src/x86/sysv.S	2026-08-12 06:32:31.020816588 +0000
+++ vendor/x86/sysv.S	2026-08-12 06:51:07.504858206 +0000
@@ -97,9 +97,14 @@
 	movl	4(%esp), %ecx
 	movl	8(%esp), %edx
 #endif
-	movl	(%esp), %eax		/* move the return address */
+	/* C2Rust lowers alloca to a heap Vec.  Preserve the real machine
+	   stack and callee-saved copy registers in extension fields appended
+	   to call_frame; the prepared heap arguments are copied below.  */
+	movl	%esp, %eax
 	movl	%ebp, (%ecx)		/* store %ebp into local frame */
-	movl	%eax, 4(%ecx)		/* store retaddr into local frame */
+	movl	%eax, 4(%ecx)		/* store caller %esp into local frame */
+	movl	%esi, 36(%ecx)
+	movl	%edi, 40(%ecx)
 
 	/* New stack frame based off ebp.  This is a itty bit of unwind
 	   trickery in that the CFA *has* changed.  There is no easy way
@@ -113,7 +118,16 @@
 	/* cfi_def_cfa(%ebp, 8) */
 	/* cfi_rel_offset(%ebp, 0) */
 
-	movl	%edx, %esp		/* set outgoing argument stack */
+	/* Build the outgoing argument area on the real stack.  This lets an
+	   arbitrary callee grow its stack normally instead of treating the
+	   C2Rust heap Vec as a machine stack.  */
+	movl	%edx, %esi
+	movl	32(%ebp), %ecx
+	subl	%ecx, %esp
+	andl	$-16, %esp
+	movl	%esp, %edi
+	rep movsb
+
 	movl	20+R_EAX*4(%ebp), %eax	/* set register arguments */
 	movl	20+R_EDX*4(%ebp), %edx
 	movl	20+R_ECX*4(%ebp), %ecx
@@ -171,15 +185,9 @@
 	/* fallthru */
 E(L(store_table), X86_RET_VOID)
 L(e1):
-	movl	8(%ebp), %ebx
-	movl	%ebp, %esp
-	popl	%ebp
-L(UW3):
-	/* cfi_remember_state */
-	/* cfi_def_cfa(%esp, 4) */
-	/* cfi_restore(%ebx) */
-	/* cfi_restore(%ebp) */
-	ret
+	/* Keep this jump-table entry below eight bytes.  The C2Rust-specific
+	   heap-frame restoration is out of line below the table.  */
+	jmp	L(return_from_heap_frame)
 L(UW4):
 	/* cfi_restore_state */
 
@@ -200,6 +208,20 @@
 E(L(store_table), X86_RET_UNUSED15)
 	ud2
 
+L(return_from_heap_frame):
+	movl	8(%ebp), %ebx
+	movl	36(%ebp), %esi
+	movl	40(%ebp), %edi
+	movl	4(%ebp), %ecx		/* recover caller %esp */
+	movl	0(%ebp), %ebp		/* recover caller %ebp */
+	movl	%ecx, %esp
+L(UW3):
+	/* cfi_remember_state */
+	/* cfi_def_cfa(%esp, 4) */
+	/* cfi_restore(%ebx) */
+	/* cfi_restore(%ebp) */
+	ret
+
 L(UW5):
 	/* cfi_endproc */
 ENDF(ffi_call_i386)
